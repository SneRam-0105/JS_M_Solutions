// Write the function multiply such that it returns the product (the result of multiplication) of a and b.

function multiplication(a, b) {
    let Z = a * b;
    return console.log(Z); // you can add console.log in return statement inside the function
}
multiplication(5, 6);

const multiplication = (a, b) => a * b;
console.log(multiplication(5, 6));

// sample usage - do not modify
console.log(multiply(2, 4));
console.log(multiply(3, 2));
